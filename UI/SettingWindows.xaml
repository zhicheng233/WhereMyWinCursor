<ui:FluentWindow
    x:Class="WhereMyWinCursor.UI.SettingWindows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
    xmlns:markup="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:pages="clr-namespace:WhereMyWinCursor.UI.Pages"
    Title="Setting"
    Width="1100"
    Height="650"
    d:DesignHeight="450"
    d:DesignWidth="800"
    markup:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    markup:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    ShowInTaskbar="True"
    mc:Ignorable="d"
>

    <Grid>
        <ui:TitleBar Title="Setting"/>

        <ui:NavigationView x:Name="NavigationView" PaneDisplayMode="LeftFluent" Margin="0,48,0,0">
            <ui:NavigationView.MenuItems>

                <ui:NavigationViewItem Content="Setting"
                                       TargetPageType="{x:Type pages:SettingPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Settings48" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="About"
                                       TargetPageType="{x:Type pages:AboutPage}">
                    <ui:NavigationViewItem.Icon>
                        <ui:SymbolIcon Symbol="Info16" />
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

            </ui:NavigationView.MenuItems>
        </ui:NavigationView>

        <ui:SnackbarPresenter x:Name="SnackbarPresenter"
                              VerticalAlignment="Bottom"
                              HorizontalAlignment="Center"
                              Margin="0,0,0,20" />

    </Grid>
</ui:FluentWindow>